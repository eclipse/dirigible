{
  "viewName":"iam_auth",
  "query":"SELECT USER_USERNAME, ROLE_ROLENAME, ASSIGN_STATE
FROM IAM_USERS, IAM_ROLES, IAM_ASSIGN
WHERE
  USER_USERNAME = ASSIGN_USERNAME
  AND ROLE_ROLENAME = ASSIGN_ROLENAME"
}