<!DOCTYPE HTML>
<html lang="en" xmlns="http://www.w3.org/1999/xhtml" ng-app="page" ng-controller="PageController">

    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="icon" href="data:;base64,iVBORw0KGgo=" dg-brand-icon />
        <title dg-brand-title></title>
        <theme></theme>
        <script type="text/javascript"
            src="/services/js/resources-core/services/loader.js?id=application-view-js"></script>
        <link type="text/css" rel="stylesheet"
            href="/services/js/resources-core/services/loader.js?id=application-view-css" />

        <script type="text/javascript" src="controller.js"></script>
    </head>

    <body class="dg-vbox">
        <fd-scrollbar class="dg-full-height">
            <div class="fd-margin--md fd-message-strip fd-message-strip--error fd-message-strip--dismissible"
                role="alert" ng-show="errorMessage">
                <p class="fd-message-strip__text">{{ errorMessage }}</p>
                <fd-button glyph="sap-icon--decline" compact="true" dg-type="transparent" aria-label="Close" in-msg-strip="true" ng-click="clearErrorMessage()">
		        </fd-button>
            </div>

            <fd-fieldset ng-form="forms.details">
                <fd-form-group dg-header="${name} Filter" name="entityForm" class="fd-container fd-form-layout-grid-container">
#foreach ($parameter in $parameters)
#if($parameter.typeTypescript == 'boolean')
                    <fd-form-item class="fd-row">
                    	<div class="fd-col fd-col-md--2"></div>
	                    <div class="fd-col fd-col-md--8">
							<fd-checkbox id="id${parameter.name}"
	                            compact="false"
								ng-model="entity.${parameter.name}">
	                        </fd-checkbox>
                        	<fd-checkbox-label for="id${parameter.name}">${parameter.name}</fd-checkbox-label>
                        </div>
                    </fd-form-item>
#elseif($parameter.typeTypescript == 'number')
                    <fd-form-item class="fd-row">
                    	<div class="fd-col fd-col-md--2">
                        	<fd-form-label for="id${parameter.name}" dg-colon="true">${parameter.name}</fd-form-label>
                        </div>
                        <div class="fd-col fd-col-md--4">
	                        <fd-step-input dg-id="id${parameter.name}"
								name="${parameter.name}"
								state="{{ forms.details['${parameter.name}'].$valid ? '' : 'error' }}"
								ng-model="entity.${parameter.name}"
								placeholder="Enter ${parameter.name}"
								>
							</fd-step-input>
	                    </div>
                    </fd-form-item>
#elseif($parameter.typeJava == 'time')
                    <fd-form-item class="fd-row">
                    	<div class="fd-col fd-col-md--2">
                        	<fd-form-label for="id${parameter.name}" dg-colon="true">${parameter.name}</fd-form-label>
                        </div>
                        <div class="fd-col fd-col-md--2">
	                        <fd-form-input-message-group dg-inactive="{{ forms.details['${parameter.name}'].$valid }}">
	                            <fd-input id="id${parameter.name}"
	                                name="${parameter.name}"
	                                state="{{ forms.details['${parameter.name}'].$valid ? '' : 'error' }}"
	                                ng-model="entity.${parameter.name}"
	                                type="time">
	                            </fd-input>
	                            <fd-form-message dg-type="error">Incorrect Input</fd-form-message>
	                        </fd-form-input-message-group>
	                    </div>
                    </fd-form-item>
#elseif($parameter.typeJava == 'timestamp')
                    <fd-form-item class="fd-row">
                    	<div class="fd-col fd-col-md--2">
                        	<fd-form-label for="id${parameter.name}" dg-colon="true">${parameter.name}</fd-form-label>
                        </div>
                        <div class="fd-col fd-col-md--3">
	                        <fd-form-input-message-group dg-inactive="{{ forms.details['${parameter.name}'].$valid }}">
	                            <fd-input id="id${parameter.name}"
	                                name="${parameter.name}"
	                                state="{{ forms.details['${parameter.name}'].$valid ? '' : 'error' }}"
	                                ng-model="entity.${parameter.name}"
	                                type="datetime-local">
	                            </fd-input>
	                            <fd-form-message dg-type="error">Incorrect Input</fd-form-message>
	                        </fd-form-input-message-group>
	                    </div>
                    </fd-form-item>
#elseif($parameter.typeJava == 'date')
                    <fd-form-item class="fd-row">
                    	<div class="fd-col fd-col-md--2">
                        	<fd-form-label for="id${parameter.name}" dg-colon="true">${parameter.name}</fd-form-label>
                        </div>
                        <div class="fd-col fd-col-md--2">
	                        <fd-form-input-message-group dg-inactive="{{ forms.details['${parameter.name}'].$valid }}">
	                            <fd-input id="id${parameter.name}"
	                                name="${parameter.name}"
	                                state="{{ forms.details['${parameter.name}'].$valid ? '' : 'error' }}"
	                                ng-model="entity.${parameter.name}"
	                                type="date">
	                            </fd-input>
	                            <fd-form-message dg-type="error">Incorrect Input</fd-form-message>
	                        </fd-form-input-message-group>
	                    </div>
                    </fd-form-item>
#else
                    <fd-form-item class="fd-row">
                    	<div class="fd-col fd-col-md--2">
                        	<fd-form-label for="id${parameter.name}" dg-colon="true">${parameter.name}</fd-form-label>
                        </div>
                        <div class="fd-col fd-col-md--8">
	                        <fd-form-input-message-group dg-inactive="{{ forms.details['${parameter.name}'].$valid }}">
	                            <fd-input id="id${parameter.name}"
	                                name="${parameter.name}"
	                                state="{{ forms.details['${parameter.name}'].$valid ? '' : 'error' }}"
	                                ng-model="entity.${parameter.name}"
	                                type="text"
	                                placeholder="Enter ${parameter.name}">
	                            </fd-input>
	                            <fd-form-message dg-type="error">Incorrect Input</fd-form-message>
	                        </fd-form-input-message-group>
	                    </div>
                    </fd-form-item>
#end
#end
                </fd-form-group>
            </fd-fieldset>
        </fd-scrollbar>

		<fd-bar bar-design="footer" ng-show="action !== 'select'" compact="true">
			<fd-bar-right>
				<fd-bar-element>
					<fd-button dg-label="Filter" dg-type="emphasized" ng-click="filter()"></fd-button>
				</fd-bar-element>
				<fd-bar-element>
					<fd-button dg-label="Reset" dg-type="transparent" ng-click="resetFilter()"></fd-button>
				</fd-bar-element>
				<fd-bar-element>
					<fd-button dg-label="Cancel" dg-type="transparent" ng-click="cancel()"></fd-button>
				</fd-bar-element>
			</fd-bar-right>
		</fd-bar>

    </body>

</html>
